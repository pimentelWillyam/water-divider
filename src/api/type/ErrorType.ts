type ErrorType = 'invalid age' | 'invalid data type' | 'invalid email' | 'name has any number' | 'name length below four letters' | 'invalid login or password'
| 'login already exists' | 'email already exists'

export type { ErrorType }
